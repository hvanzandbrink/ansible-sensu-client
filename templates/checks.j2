{
  "checks": {
{% if texts is defined %}
{% for website in websites %}
    "{{ website.name }}": {
      "command": "/etc/sensu/plugins/check-http.rb -u {{ website.url }} -q '{{ website.keyword }}'",
      "standalone": true,
      "interval": 60,
      "refresh": 60      
    },
{% endfor %}
{% endif %}
{% for check_metric in checks_metrics %}
    "{{ check_metric }}": {
      "type": "{{ check_metric | regex_replace('^(.*)-.*', '\\1') }}",
    {% if {{ check_metric | regex_replace('^(.*)-.*', '\\1') }} is 'metrics' %}
      "handlers": ["relay"],
    {% endif %}
      "command": "{{ check_metric }}.rb",
      "standalone": true,
      "interval": 60,
      "refresh": 60
    }
{% if not loop.last %}
,
{% endif %}
{% endfor %}
  }
}
